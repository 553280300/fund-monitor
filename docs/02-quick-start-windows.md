# Windows 快速开始

1. 解压交付包，双击 `FundMonitor.exe`。
2. 首次启动会在浏览器打开本地管理面板；请等待最多 30 秒。
3. 在“添加资产”填写名称、类型、数据源标识和代码。
4. 在“添加涨跌告警”选择资产，填写阈值和冷却时间。
5. 使用每个资产右侧的“检查”确认数据源能返回结果。

应用默认只监听本机 `127.0.0.1`，无需安装 Python。运行数据保存到 `%LOCALAPPDATA%\FundMonitor`。
